export interface StudyRoom {
  body: string;
  categoryName: string;
  conversation: string;
  expiredAt: string;
  id: string;
  maxuser: number;
  openAt: string;
  title: string;
  joinUser: number;
}
