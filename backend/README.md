##  Project Architecture

본 프로젝트는 **Layered Architecture**와 **Clean Architecture**를 기반으로 설계되었습니다.

interfaces → service → domain ← infras


---

### 📦 interfaces layer

- 외부와의 입출력을 담당하는 계층입니다.
- HTTP API 요청/응답 처리, Controller, Request/Response DTO 등이 위치합니다.

---

### 📦 service layer

- 핵심 **비즈니스 로직**을 담당하는 계층입니다.
- `domain` layer의 모델과 인터페이스를 활용하여 요구사항을 구현합니다.

---

### 📦 domain layer

- 프로젝트의 **핵심 도메인 모델**을 정의하는 계층입니다.
- JPA Entity, Enum, Domain Service, Repository **인터페이스** 등이 위치합니다.
- 도메인 로직에 집중하며, 외부 기술에 의존하지 않습니다.

---

### 📦 infras layer

- 기술적인 **구현체**가 위치하는 계층입니다.
- `domain` 계층에 선언된 Repository 인터페이스의 **구현체**를 이곳에 정의합니다.
- DB, 외부 API, 메시징 시스템 등 인프라 스트럭처와의 연결을 담당합니다.

---

### 📌 설계 원칙

- **상위 계층은 하위 계층을 의존할 수 있지만**,  
  **하위 계층은 상위 계층을 절대 의존하지 않습니다.**

- 모든 Repository 인터페이스는 `domain` layer에 정의하고,  
  구현체는 반드시 `infras` layer에 구현해주세요.
            
이를 통해 **DIP (Dependency Inversion Principle)** 을 준수합니다.